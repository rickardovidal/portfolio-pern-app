# === DEPENDÊNCIAS ===
# Estas pastas contêm milhares de ficheiros baixados automaticamente
# Seria impraticável e desnecessário versionar todos eles
node_modules/
*/node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# === VARIÁVEIS DE AMBIENTE E CONFIGURAÇÕES SENSÍVEIS ===
# Estes ficheiros contêm passwords e chaves secretas
# NUNCA devem ser enviados para repositórios públicos
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*/.env
*/.env.local

# === FICHEIROS DE BUILD E DISTRIBUIÇÃO ===
# Estas pastas são geradas automaticamente durante o processo de build
# As plataformas de deploy criam-nas novamente, por isso não precisamos delas no Git
build/
dist/
*/build/
*/dist/

# === LOGS E FICHEIROS TEMPORÁRIOS ===
# Ficheiros criados automaticamente durante o desenvolvimento
*.log
logs/
pids/
*.pid
*.seed
*.pid.lock

# === CACHE E FICHEIROS TEMPORÁRIOS ===
# Informação temporária que pode ser recriada automaticamente
.cache/
.parcel-cache/
.npm/
.eslintcache

# === SISTEMAS OPERATIVOS ===
# Ficheiros criados automaticamente por diferentes sistemas operativos
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# === EDITORES DE CÓDIGO ===
# Configurações específicas do teu ambiente de desenvolvimento
.vscode/
.idea/
*.swp
*.swo
*~

# === OUTROS ===
# Ficheiros de teste e documentação temporária
coverage/
.nyc_output/
*.tgz
*.tar.gz