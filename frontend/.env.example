# Copiar para .env.local para desenvolvimento local (o .env.local não vai para o git)
# URL base da API do backend. Sem esta variável, o frontend usa a produção no Render.
VITE_API_URL=http://localhost:3000/api
